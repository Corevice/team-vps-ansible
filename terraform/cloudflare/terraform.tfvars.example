# Copy to terraform.tfvars and fill in real values.
# terraform.tfvars is gitignored (never commit secrets).

# CF API token (Tunnel:Edit + Access:Edit + DNS:Edit + Zone:Read)
cloudflare_api_token = ""  # FILL IN
